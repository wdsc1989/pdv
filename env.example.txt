# Exemplo de configuração de ambiente para o PDV
# Copie este arquivo para .env e ajuste conforme o ambiente.

# Ambiente de desenvolvimento local (opção 1 - SQLite, mais simples)
# Se DATABASE_URL não for definido, o sistema usará automaticamente SQLite em data/pdv.db
#DATABASE_URL=

# Ambiente de desenvolvimento local (opção 2 - PostgreSQL local)
#DATABASE_URL=postgresql://pdv_user:SENHA_AQUI@localhost:5432/pdv_db

# Ambiente de produção (Hostinger/PostgreSQL)
#DATABASE_URL=postgresql://pdv_user:SENHA_FORTE_AQUI@HOST:PORTA/pdv_db

# Secret para sessões/autenticação (use um valor aleatório grande em produção)
#SECRET_KEY=troque-para-uma-chave-secreta-segura

